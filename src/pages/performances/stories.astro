---
import AccordionItem from "../../components/AccordionItem.astro";
import Layout from "../../layouts/Layout.astro";
import { getServiceItems } from "../../utils/sanity";

const stories = await getServiceItems("performances-narratives");
---

<Layout
  title="Dramatic Narrative Performances | Scripture Alive"
  description="Dramatic narrative performances — the One-Man Passion Play, The Advent Story, The Beginning and the End, and more."
  ogImage={"https://cdn.sanity.io/images/vxczpihg/production/80d50746c21de9f688034d67ed88bae2efe55536-1024x682.png"}
>

  <!-- Back Button -->
  <div class="max-w-5xl mx-auto px-4 pt-6">
    <a href="/performances" class="inline-flex items-center gap-2 text-sm font-bold uppercase tracking-widest text-gray-700 hover:text-accent transition-colors">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
      Back to Performances
    </a>
  </div>

  <!-- Hero -->
  <section class="py-10 px-4 bg-white text-center">
    <div class="max-w-5xl mx-auto">
      <p class="text-sm font-bold uppercase tracking-widest text-primary mb-2">
        <a href="/performances" class="hover:underline">Performances</a> / Narratives
      </p>
      <h1 class="text-3xl md:text-4xl font-bold text-accent mb-4">
        Narratives
      </h1>
      <p class="text-base md:text-lg text-gray-600 leading-relaxed">
        Dramatic Scripture performances weaving multiple voices and perspectives into a single powerful story.
      </p>
    </div>
  </section>

  <!-- Accordion Items -->
  <section class="py-10 px-4 bg-cream">
    <div class="max-w-5xl mx-auto flex flex-col gap-4">
      {stories.map((story) => (
        <AccordionItem
          title={story.title}
          imageUrl={story.imageUrl}
          imageAlt={`${story.title} Performance`}
          description={story.description}
          ctaHref="/book-jeremy"
        />
      ))}
    </div>
  </section>

  <!-- CTA -->
  <section class="py-12 px-4 bg-white text-center">
    <div class="max-w-5xl mx-auto">
      <h2 class="text-2xl font-bold text-primary font-serif mb-4">
        Ready to bring Scripture to life at your event?
      </h2>
      <a
        href="/book-jeremy"
        class="inline-block bg-primary text-gray-900 font-bold px-8 py-3 rounded hover:opacity-90 transition-opacity"
      >
        Book a Performance
      </a>
    </div>
  </section>

</Layout>
